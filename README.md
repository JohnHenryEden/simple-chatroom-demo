# A simple chat room project that demonstrates Java socket usage.

How to use:
- Run the main method in Server
- Run the main method in Client, can run several ones at the same time
- Available commands:
  - bye: Logout
  - toall: Send to all users
  - touser <username>: Send to a specific user
  - setname: Set user name

Can be extended by adding new strategy.
